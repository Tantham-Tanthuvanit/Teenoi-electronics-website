git pull

create a new branch
git checkout -b feature/login-screen

switch branch
git checkout branch-name

see branches in project
git branch

add changes to current branch
git add .
git commit -m "message"
git push origin branch-name